# Sandwich Bot

### step-4
```
filter out the swap logs from these flattend logs
> filterout the pools
> filter out the weth pools
we will keep the uniswap v2 pools and tokens in cached csv file for fast loading when we restart the system.
it’ll start printing the swap info
we can find target_pair, main_currency, target_token, and the correct swap direction of the pending transactions in the swap info.
```